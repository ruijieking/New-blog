<main>
	<section>
		<h1 class="page-title">📸 照片墙</h1>
		<p class="page-desc">记录生活中的美好瞬间</p>
		<div class="photo-grid">
			<div class="photo-placeholder">
				<span class="placeholder-icon">原神nb</span>
			</div>
		</div>
	</section>
</main>

<style>
	main {
		width: 960px;
		max-width: calc(100% - 2em);
		margin: auto;
		padding: 3em 1em;
	}
	.page-title {
		font-size: 2.5em;
		text-align: center;
		margin-bottom: 0.5em;
	}
	.page-desc {
		text-align: center;
		color: rgb(var(--gray));
		margin-bottom: 2em;
		font-size: 1.1em;
	}
	.photo-grid {
		display: grid;
		grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
		gap: 1.5rem;
	}
	.photo-placeholder {
		grid-column: 1 / -1;
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		padding: 4em 2em;
		border: 2px dashed rgb(var(--gray-light));
		border-radius: 16px;
		color: rgb(var(--gray));
		font-size: 1.2em;
		gap: 1em;
	}
	.placeholder-icon {
		font-size: 3em;
	}
</style>